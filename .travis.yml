env:
  global:
    - REPO="ionicapp"
    - CI_HOME=`pwd`/$REPO

language: node_js

install:
  - npm install -g firebase-tools
  - npm install -g cordova
script:
  - cd ionicapp
  - ionic build web
after_success:
  - firebase deploy --token ${FIREBASE_TOKEN}
