import {Area} from './area';
import {Category} from './category';
import {Item} from './item';

export class Optional {
  nr: number;
  desc: string;
  area: Area;
  category: Category;
  item: Item;
}
